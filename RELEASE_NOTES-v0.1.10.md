# ChatGPT Prompt Scheduler Android v0.1.10

## 프로젝트 새 대화 중복 생성 방지

- 프로젝트 시작 화면에서 입력 또는 모드 처리 중 같은 프로젝트의 새 `/c/...` 대화 URL이 만들어지면 이를 대상 이탈로 간주해 루트로 되돌리던 로직을 수정했습니다.
- 같은 프로젝트의 새 대화에 아직 사용자 메시지가 없으면 현재 실행이 생성한 정상 진행 경로로 승계하고, 해당 대화에서 한 번만 전송합니다.
- 새 대화에 동일 실행 프롬프트가 이미 존재하면 다시 입력하거나 전송하지 않고 전송 완료 상태로 전환합니다.
- 관련 없는 기존 프로젝트 대화에 사용자 메시지가 존재하는 경우에는 기존처럼 대상 불일치로 판정해 잘못된 대화에 이어 쓰지 않습니다.

## 모드 선택 안전성

- Chat·Work 모드 선택을 부분 문자열 검색에서 정확한 레이블 비교로 변경했습니다.
- `새 채팅`, `새 대화`, `New chat`, `New conversation` 요소는 모드 후보에서 명시적으로 제외합니다.
- 실행별 모드 클릭 마커를 저장해 페이지 전환 후 같은 모드 요소를 반복 클릭하지 않습니다.
- 실행 풀로그에 대상 경로와 모드 후보·선택·클릭 진단을 함께 기록합니다.

## 검증

- 같은 프로젝트의 빈 새 대화 승계, `새 채팅` 제외, 관련 없는 기존 대화 거부, 기존 대화 대상의 엄격한 URL 검증을 회귀 테스트로 추가했습니다.
- versionCode: 11
- versionName: 0.1.10
- 기존 고정 서명 인증서를 유지합니다.
