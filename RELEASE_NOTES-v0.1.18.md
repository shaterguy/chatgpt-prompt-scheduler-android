# v0.1.18

오토런 `새로 시작`이 초안·주소 보호 조건에 가로막혀 시작 문구를 보내지 못하던 문제를 수정했습니다.

- `새로 시작`은 일반 Chat 입력창의 기존 초안을 지우고 정확한 `[AUTOMATION_START <JOB_ID>]`로 덮어씁니다.
- 같은 Job ID의 과거 시작 턴이 있더라도 새 시작 문구를 새 사용자 턴으로 한 번 전송합니다.
- 전송 전 동일 시작 턴 개수를 기준선으로 저장하고, 클릭 뒤 개수가 증가한 경우에만 실제 새 사용자 턴으로 확인합니다.
- 최초 시작 중 같은 conversation ID를 유지하는 ChatGPT 정규화·SPA 주소 변경은 `TARGET_CHANGED`로 처리하지 않습니다.
- 새 시작 사용자 턴을 확인한 즉시 정규화된 주소를 고정하고 기존 초안 보호·중복 방지·대상 검사로 복귀합니다.
- 같은 Job을 중간부터 다시 시작하면 첫 일반 Chat 제어 신호의 Step/Round를 Drive 진행 위치로 한 번만 재설정하고, 이후 기존 엄격 순서 검증으로 복귀합니다.
- 최초 대상 주소가 일시적으로 홈·빈 경로를 거칠 때 즉시 중단하거나 반복 로드하지 않고 적응형 백오프로 원래 대화를 계속 엽니다.
- 예약 실행 우선, AlarmEngine, QueueStore, ExecutionService 및 기존 예약 데이터 경로는 변경하지 않았습니다.
- versionCode는 19, versionName은 0.1.18입니다.
- v0.1.17과 동일한 공개 서명 identity를 사용해 덮어쓰기 업데이트를 유지합니다.
