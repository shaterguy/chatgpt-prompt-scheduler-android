# ChatGPT Prompt Scheduler Android v0.1.6

## 수정 사항

- 프로젝트·기존 대화 실행 직전에 실제 WebView URL의 프로젝트 ID와 대화 ID를 검증합니다.
- 일반 ChatGPT 홈이나 다른 대화로 이탈하면 프롬프트를 입력하지 않고 원래 대상 URL로 최대 3회 자동 복구합니다.
- 복구 실패 시 `TARGET_ROUTE_RECOVERY_FAILED`와 요청 URL·실제 URL을 기록합니다.
- 같은 예약이 실행 중이거나 대기 중일 때 `지금 실행`을 반복해서 눌러도 중복 작업을 추가하지 않습니다.
- `onPageFinished`와 SPA URL 변경 콜백에서 중복 자동화 작업이 쌓이지 않도록 단일 예약 콜백으로 정리했습니다.
- 페이지 이동 때마다 재시도 횟수가 초기화되던 문제를 제거했습니다.
- 실행 기록을 비동기 `apply()`가 아닌 동기 `commit()`으로 저장하고 이전 기록 백업을 유지합니다.
- 실행 기록 JSON이 손상되면 백업을 복구하며, 복구할 수 없을 때 빈 화면 대신 `LOG_STORAGE_CORRUPT` 진단 기록을 표시합니다.
- 실행 기록 저장 실패 시 예약 카드의 마지막 상태에 `_LOG_SAVE_FAILED`를 표시합니다.
- 분 간격 예약의 실제 다음 실행 시각을 예약 데이터에 저장합니다.
- 앱을 열거나 화면을 새로 표시할 때 `현재 시각 + 간격`을 다시 계산하지 않고, AlarmManager에 등록한 실제 다음 실행 시각을 그대로 표시합니다.
- 재부팅·앱 업데이트로 알람을 재구축할 때도 아직 지나지 않은 다음 실행 시각은 유지합니다.

## 업데이트

- versionCode: 7
- versionName: 0.1.6
- v0.1.2 이후와 동일한 고정 서명 인증서를 사용하므로 기존 앱 위에 업데이트 설치할 수 있습니다.
