# v0.1.15

Protocol 3.x 오토런 중계의 장시간 실행과 복구 신뢰성을 수정한 릴리스입니다.

- 정상 Work/일반 Chat 응답은 시간 제한 없이 계속 감시합니다.
- 신호 수신, 전달 준비, 제출 시도, DOM 제출 확인, 응답 대기를 영속 상태로 분리했습니다.
- 프로세스 또는 WebView 렌더러 재시작 시 이미 존재하는 동일 사용자 턴을 확인하며 자동 재클릭하지 않습니다.
- 현재 모니터링 방, 현재 전달, 마지막 확정 전달, 마지막 신호, 다음 기대, 오류를 화면에서 분리합니다.
- 규정 외 최종 응답과 명확한 WebView/네트워크 오류는 민감한 응답 본문 없이 Android 알림으로 안내합니다.
- `AR_USER_ACTION_REQUIRED`와 `AUTOMATION_USER_RESOLVED` 재검증 흐름을 추가했습니다.
- 앱 화면 이동은 중계 상태를 변경하지 않으며, 홈의 `오토런 중계 열기` 버튼을 상단으로 옮겼습니다.
- 예약 실행은 계속 오토런보다 우선하며 기존 예약 저장·알람·대기열·실행 경로는 변경하지 않았습니다.

공개 APK는 v0.1.14와 같은 인증서로 서명하며 versionCode 16으로 덮어쓰기 업데이트할 수 있습니다.
